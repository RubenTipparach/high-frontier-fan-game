{
  "name": "high-frontier-fan-game-web",
  "version": "2.0.0",
  "private": true,
  "description": "Static frontend build (esbuild) for High Frontier fan game.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check-boot.mjs",
    "icons": "node scripts/gen-map-icons.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
